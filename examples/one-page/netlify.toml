[build]
    command = "cd ../../ && pnpm install && pnpm run build && pnpm --filter one-page run build"
    publish = "build"

[build.environment]
  NODE_VERSION = "24"

[functions]
  node_bundler = "esbuild"