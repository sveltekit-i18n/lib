[build]
    command = "cd ../../ && pnpm install && pnpm run build && pnpm --filter preprocess run build"
    publish = "build"

[build.environment]
  NODE_VERSION = "24"

[functions]
  node_bundler = "esbuild"